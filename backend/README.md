# Backend (Django) — LifeCore

Apps previstas:
- apps/agents_v2: endpoints y orquestación de agentes.
- apps/memory: `MemoryManager` (short/mid/long).
- apps/consent: `SmartConsent` (middleware/decoradores) y auditoría.
- apps/timeline: registro de eventos (ingesta, respuestas, adherencia, síntomas).
- apps/api: routers v2 y health/status.

Requirements: ver `requirements.txt` (pendiente de consolidar desde backend actual).
